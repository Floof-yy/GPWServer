# GPWServer
General Purpose Web Server (Or GPWS) is a gateway platform for current and future projects that are web based. Currently it's planned to 'host'* a file transfer server. Things will probably be hosted on a raspberry

[* I realise now an hour later that github pages doesn't support FTP, i guess i'll just make it a link to a seperate SFTP server using SSH.. Raspberry pi?]: #
